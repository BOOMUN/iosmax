# Deprecated build path

The Dopamine package project moved to:

```text
device/virtual-camera/rootless/
```

This directory is retained only so historical `.theos` build output is not
mistaken for RootHide output. Do not publish packages from this path.
